<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="DialogStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SeparatorTextStyle" parent="@android:style/Widget.TextView">
        <item name="android:background">@color/separator_background</item>
        <item name="android:textColor">?android:textColorPrimaryInverse</item>
        <item name="android:paddingLeft">8dp</item>
        <item name="android:paddingRight">8dp</item>
        <item name="android:paddingTop">10dp</item>
        <item name="android:paddingBottom">10dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="Steerpath.Style.LevelPicker.Text.Selected" parent="@android:style/Widget.TextView">
        <item name="android:textColor">@color/icon</item>
        <!-- background needs to be @color -->
        <item name="android:background">@color/colorAccent</item>
    </style>

    <style name="Steerpath.Style.LevelPicker.Text.Unselected" parent="@android:style/Widget.TextView">
        <item name="android:textColor">@color/colorAccent</item>
        <!-- background needs to be @color -->
        <item name="android:background">@color/icon</item>
    </style>

    <style name="Steerpath.Style.LevelPicker.Text.Transition" parent="@android:style/Widget.TextView">
        <item name="android:textSize">60sp</item>
    </style>

    <style name="Steerpath.Style.Map.ProgressBar" parent="Widget.AppCompat.ProgressBar.Horizontal">
        <item name="android:background">@color/divider</item>
        <!--<item name="android:layout_margin">0dp</item>
        <item name="android:padding">0dp</item>-->
        <item name="android:indeterminate">true</item>
    </style>

</resources>
